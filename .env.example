# Backend environment variables
# Path to YOLO model weights (relative or absolute)
PPE_MODEL=https://drive.google.com/file/d/1xpgEp5eu4r6Xz_bc_01OkI6cnlPucmJf/view?usp=drive_link
# Comma-separated list of allowed CORS origins (Render frontend origin will be injected at deploy time)
CORS_ORIGINS=http://localhost:5173
# Host and port (optional overrides)
HOST=0.0.0.0
PORT=8000

# Frontend build-time variables (Vite requires VITE_ prefix)
VITE_API_BASE=http://localhost:8000
